// take a user input in print the odd numbers ande even numbers?.  use with in if-else methods (ternary method)

#include<iostream>
using namespace std;
int main(){
    int n;
    cout<<"enter the numbers : ";
    cin>>n;
    (n%2==0) ? cout<<"even" : cout<<"odd";  // (ternary method)
}
